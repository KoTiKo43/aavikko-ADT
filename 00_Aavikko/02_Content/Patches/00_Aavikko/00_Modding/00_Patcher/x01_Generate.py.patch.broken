diff --git a/00_Aavikko/00_Modding/00_Patcher/x01_Generate.py b/00_Aavikko/00_Modding/00_Patcher/x01_Generate.py
index 9b5790e8ad..662cc84d83 100644
--- a/00_Aavikko/00_Modding/00_Patcher/x01_Generate.py
+++ b/00_Aavikko/00_Modding/00_Patcher/x01_Generate.py
@@ -605,6 +605,18 @@ def main():
             f"Saved {saved}/{len(modified)} file(s)",
             next_step=("All upstream files restored to HEAD." if args.restore else None),
         )
+
+        # ── Post-step: regenerate showcase map ──
+        try:
+            import generate_showcase_map
+            saved_argv = sys.argv[:]
+            sys.argv = [sys.argv[0]]
+            generate_showcase_map.main()
+            sys.argv = saved_argv
+        except Exception as e:
+            print(f"  [WARN] Showcase map generation failed: {e}", file=sys.stderr)
+            import traceback
+            traceback.print_exc(file=sys.stderr)
         return
     
     if not args.filepath:

